---
layout: default
title: "Flappy Bird — Class 7 — Done!"
---

# 🏆 Class 7 done!

Pipes spawning forever! ✅

## What you did
- Replaced one pipe with `pipes` — a list of `Pipe` objects
- Used `random.randint` to pick fresh gap heights
- Added a spawn timer + cleanup loop for off-screen pipes

---

## Next class → 🧮 Score!

You're flying through pipes — now they should **count**. Every
pipe you fly past should bump a score that shows at the top of
the screen.

In Class 8 you'll add a `passed` attribute to each pipe (so each
pipe is only counted once), a `score` counter, and a HUD pen
that writes `Score: N` at the top of the window.

<p style="text-align:center;margin:2.5em 0;">
  <a href="../../index.html" style="display:inline-block;background:#FFD23F;color:#1a1a1a;padding:14px 28px;border-radius:8px;text-decoration:none;font-size:1.25em;font-weight:bold;">⬅ Back to all tutorials</a>
</p>
