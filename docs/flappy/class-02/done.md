---
layout: default
title: "Flappy Bird — Class 2 — Done!"
---

# 🏆 Class 2 done!

You wrote your first class. ✅

## What you did
- Wrote `class Bird:` with `__init__` and `draw`
- Each bird gets its **own pen** (`self.pen`)
- Drew body + eye + beak using `begin_fill` / `forward` / `left`

---

## Next class → 🪂 Gravity

The bird is hanging in the air doing nothing. In real Flappy Bird
the bird **falls** every frame unless you flap.

In Class 3 you'll add a `vy` (vertical velocity) attribute, an
`update()` method that applies gravity, and a **game loop** that
runs `update()` and `draw()` 30 times a second using
`screen.ontimer`.

The bird will start dropping like a stone.

<p style="text-align:center;margin:2.5em 0;">
  <a href="../../index.html" style="display:inline-block;background:#FFD23F;color:#1a1a1a;padding:14px 28px;border-radius:8px;text-decoration:none;font-size:1.25em;font-weight:bold;">⬅ Back to all tutorials</a>
</p>
