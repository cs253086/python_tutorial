---
layout: default
title: "Flappy Bird — Class 9 — Done!"
---

# 🏆 Class 9 done!

Crashes work. ✅

## What you did
- Wrote `collides(b, p)` with four early `return` checks
- Stopped the game with `return` inside `tick`
- Added a ceiling/floor check too

---

## Next class → 🎮 Game over + restart

The game freezes on a crash, but a real game **shows GAME OVER**
and lets you tap a key to **start again**.

In Class 10 you'll add a `running` flag, a `game_over()` function
that draws the message, and a `restart()` function that resets
the bird, the pipe list, and the score so you can play again.

<p style="text-align:center;margin:2.5em 0;">
  <a href="../../index.html" style="display:inline-block;background:#FFD23F;color:#1a1a1a;padding:14px 28px;border-radius:8px;text-decoration:none;font-size:1.25em;font-weight:bold;">⬅ Back to all tutorials</a>
</p>
