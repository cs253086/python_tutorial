# Exercise: Input Practice

Complete each part in order.

## Part A: Ask and greet

Create `greet_player.py`:

1. Ask for player name
2. Print a welcome message using that name

Run:

```bash
python3 greet_player.py
```

---

## Part B: Choose a path

Create `path_choice.py`:

1. Ask: `"Choose a path (forest/cave): "`
2. Print what the player chose

---

## Part C: Number input

Create `set_lives.py`:

1. Ask for number of lives
2. Convert input to integer using `int(...)`
3. Print the starting lives

Example output:

```text
Starting lives: 3
```

---

## Part D: Fill in the blanks

Complete this code:

```python
player_name = ________("What is your name? ")
print("Hello,", ____________)

move = input("Move (left/right): ")
print("You chose:", ________)

lives = ____ (input("Lives: "))
print("Lives:", lives)
```

---

## Part E: Fix the bugs

Fix this code so it runs:

```python
name = input What is your name?
print("Hi", name
move = inpt("Move: ")
print("Move:", move)
```

---

## Success checklist

- [ ] I can use `input()` with a prompt
- [ ] I can store user answers in variables
- [ ] I can convert input text to integer with `int()`
- [ ] I can fix basic input mistakes
