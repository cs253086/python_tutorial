# Exercise: List Practice

Complete each part in order.

## Part A: Favorite items list

Create `favorites.py`:

1. Create a list with 4 favorite things
2. Print each item using indexes

---

## Part B: Inventory actions

Create `inventory_actions.py`:

1. Start with: `["torch", "rope"]`
2. Add `"key"` using `append`
3. Change `"rope"` to `"strong rope"`
4. Print the final list

---

## Part C: Loop through list

Create `inventory_loop.py`:

1. Make a list of 5 items
2. Use `for item in ...` to print each item

---

## Part D: Fill in the blanks

```python
rooms = ["start", "empty", "trap", "treasure"]
print(rooms[__])
rooms._______("exit")
rooms[1] = "safe"

for room in ______:
    print(room)
```

---

## Part E: Fix the bugs

Fix this code:

```python
items = "torch", "rope", "key"
print(items[0]
items.append["map"]
for i in items
    print(i)
```

---

## Success checklist

- [ ] I can create and print lists
- [ ] I can append and update items
- [ ] I can loop through a list
- [ ] I can fix common list syntax issues
