# Exercise: Function Practice

Complete each part in order.

## Part A: Simple function

Create `hello_function.py`:

1. Define `say_hi()` that prints a greeting
2. Call it two times

---

## Part B: Function with parameter

Create `badge.py`:

1. Define `show_badge(name)`
2. Print: `"Explorer badge for <name>"`
3. Ask for name with `input()` and call function

---

## Part C: Function with return

Create `score_updater.py`:

1. Define `add_points(score, points)` returning new score
2. Start with score 0
3. Add 10, then add 5
4. Print final score

---

## Part D: Fill in the blanks

```python
def greet(____):
    print("Hello,", name)

def add(a, b):
    return a + __

player = input("Name: ")
____(player)

score = add(10, 5)
print(score)
```

---

## Part E: Fix the bugs

Fix this code:

```python
def show_title
    print("Treasure Hunt")

def add_score(score, points):
    new_score = score + points

score = 0
score = add_score(score, 10)
print(score)
```

---

## Success checklist

- [ ] I can define and call functions
- [ ] I can pass arguments
- [ ] I can use return values
- [ ] I can fix common function syntax errors
